import React, { Component } from 'react';
import '../../styles/GeneralQuestions.css';

export default class GeneralQuestions extends Component {
  render() {
    return (
      <div className="general">
      </div>
    );
  }
}
